# Upmarket Pro Tier Dependencies
#
# These are the core conversion libraries needed for Enhanced conversion:
# - Layout analysis and table extraction
# - Office format handling
# - All Basic tier capabilities
#
# Explicitly does NOT include ML frameworks (torch, mlx, transformers).
# Those are downloaded separately as aiLibraries for Max tier users.

# Core conversion (Docling without AI)
docling==2.96.0
docling-core==2.78.0
docling-parse==6.2.0
docling-ibm-models==3.13.2
markitdown==0.1.6

# Office format handlers
pypdfium2==5.8.0
mammoth==1.11.0
python-pptx==1.0.2
openpyxl==3.1.5

# Native macOS OCR (Apple Vision framework integration)
ocrmac==1.0.1

# Utilities (image processing and data handling)
pydantic==2.13.4
Pillow==12.2.0
numpy==2.3.5
