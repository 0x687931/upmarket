# Upmarket Python dependencies
# ALL versions must be pinned before bundling into the app.
# Resolve the mlx-vlm / docling-ibm-models transformers conflict before finalising.
#
# To resolve: install both and run:
#   pip install docling mlx-vlm && pip check

# Core conversion
docling>=2.92.0
docling-core>=2.70.0
docling-parse>=5.3.2

# ML inference
torch>=2.2.2,<3.0.0          # MPS backend for Apple Silicon
torchvision>=0.17.0

# MLX for Apple Silicon VLM (SmolDocling)
mlx>=0.18.0
mlx-vlm>=0.1.0               # pin carefully — transformers conflict with docling-ibm-models

# HuggingFace (model download only — offline after first run)
huggingface_hub>=0.23.0
transformers>=4.40.0          # must satisfy both mlx-vlm and docling-ibm-models

# PDF + document parsing
pypdfium2>=4.30.0

# Utilities
pydantic>=2.0.0
Pillow>=10.0.0
