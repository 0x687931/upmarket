# Upmarket release-current Python dependencies.
#
# This file is the current release state. Every runtime dependency listed here
# must use an exact == pin. Do not use >=, ~=, <, >, or unbounded specifiers.
# Proposed updates go in requirements-candidate.txt first.

# Core conversion
docling==2.96.0
docling-core==2.78.0
docling-parse==6.2.0
docling-ibm-models==3.13.2
markitdown==0.1.6

# Fast PDF and Office fallbacks
pypdfium2==5.8.0
mammoth==1.11.0
python-pptx==1.0.2
openpyxl==3.1.5

# Model and ML runtime
torch==2.12.0
torchvision==0.27.0
transformers==5.9.0
huggingface_hub==1.17.0
mlx==0.31.2
mlx-metal==0.31.2
mlx-vlm==0.3.3

# Native macOS OCR (wraps Apple Vision framework — no extra model downloads)
ocrmac==1.0.1

# Utilities
pydantic==2.13.4
Pillow==12.2.0
numpy==2.3.5

# HuggingFace dataset evaluation (optional for benchmark suite)
webdataset==0.2.86
editdistance==0.8.1
pdfplumber==0.11.4
