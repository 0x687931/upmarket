# Upmarket release-candidate Python dependencies.
#
# Keep this file exact-pinned too. Update this file when evaluating upstream
# releases, then run dependency audit, packaged import/offline smoke, and
# corpus validation before promoting the same pins to requirements.txt.

# Core conversion
docling==2.96.0
docling-core==2.78.0
docling-parse==6.2.0
docling-ibm-models==3.13.2
markitdown==0.1.6

# Fast PDF and Office fallbacks
pypdfium2==5.8.0
mammoth==1.11.0
python-pptx==1.0.2
openpyxl==3.1.5

# Model and ML runtime
torch==2.12.0
torchvision==0.27.0
transformers==5.9.0
huggingface_hub==1.17.0
mlx==0.30.4
mlx-metal==0.30.4

# Utilities
pydantic==2.13.4
Pillow==12.2.0
numpy==2.4.6
