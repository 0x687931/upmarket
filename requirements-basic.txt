# Upmarket Basic Tier Dependencies (Bundled in App)
#
# This is the MINIMAL Python environment bundled inside Upmarket.app.
# Size target: ~50MB
#
# Includes:
# - Tier-1 conversion: MarkItDown (office/markup) + pypdfium2 (native PDF text)
# - Image processing (Pillow)
# - Basic data structures (numpy, pydantic)
#
# Does NOT include:
# - Docling (requires Pro download — enhanced PDF layout/tables)
# - ML frameworks (torch, mlx, etc. - Max only)
# - ocrmac / pyobjc: native OCR, Vision, and Speech run in Swift against the system
#   frameworks (no Python bindings). ocrmac is Docling's optional OCR backend → Pro.

# Tier-1 (zero-download) Basic conversion: MarkItDown + mammoth/lxml handle DOCX, HTML,
# CSV; pypdfium2 backs native PDF text. Spreadsheets/presentations/ebooks (pandas,
# openpyxl, python-pptx, xlrd) are Pro — see the AppTier contract / converter.py.
markitdown==0.1.6
mammoth==1.11.0
lxml==6.1.1
pypdfium2==5.8.0

# Utilities
pydantic==2.13.4
Pillow==12.2.0
numpy==2.3.5
